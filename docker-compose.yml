services:
  db:
    image: postgres:16
    restart: "unless-stopped"
    environment:
      POSTGRES_USER: dais
      POSTGRES_PASSWORD: dais
      POSTGRES_DB: dais
    ports:
      - "51992:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

  web:
    build: .
    command: npm run dev
    environment:
      PORT: 3001
    ports:
      - "51991:3001"
    volumes:
      - ./:/app
      - /app/node_modules
    env_file:
      - .env.docker
    depends_on:
      - db

volumes:
  pgdata:
