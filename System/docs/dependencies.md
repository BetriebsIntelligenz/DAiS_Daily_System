# Dependencies

- Python 3.11+
- Runtime nutzt ausschliesslich Standardbibliothek.
- Dev-Tools: `pytest`, `ruff`, `mypy`, `build` (siehe `pyproject.toml`).

Neue Abhaengigkeiten muessen hier dokumentiert und in `requirements.txt`
verankert werden.

